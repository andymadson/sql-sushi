# Copy to .env and fill in. The runner reads these via os.environ.
# Don't commit .env. The .gitignore in this repo already blocks it.

PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=sqlsushi
PG_USER=sqlsushi
PG_PASSWORD=sqlsushi
